# Cloudflare Turnstile (antibot) — https://dash.cloudflare.com → Turnstile
# Public site key, safe to expose to the browser.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=
# Secret key, server-only (set in Vercel env vars; never in the repo).
TURNSTILE_SECRET_KEY=
